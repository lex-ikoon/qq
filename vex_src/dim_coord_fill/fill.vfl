///------------------------------------------------------------------------------------------------
/// fill to coord

#include "qq.vfl"

float dimension = @dim;
// float coord = @coord;
float coord  = chf("coord");   // 0.5 in 0.0 to 1.0
float spread = chf("spread");  // 0.5 in 0 to 1

@fill = fill_to_coord(dimension, coord, spread);
@Cd   = @fill;