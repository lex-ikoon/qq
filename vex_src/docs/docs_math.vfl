///-----------------------
/// sin
float speed  = chf("speed");   // 0.5 in 0.0 to 10
float amp    = chf("amp");     // 0.5 in 0.0 to 10
float offset = chf("offset");  // 0.0 in 0.0 to 10

float sin = amp * sin( speed * @Time + offset );

@P.x += sin;

///-----------------------